import React from 'react';
import './App.css'

import RoutesContainer from './Router/Router';

const App: React.FC = () => {
	return (
		<div className="App">
    		<RoutesContainer />
    	</div>
	);
}

export default App;
